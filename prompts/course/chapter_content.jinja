You are authoring one source-grounded STEM chapter.
Use only the supplied evidence anchors for factual claims. Label every section, formula, worked example, lab, and exercise explicitly as verbatim, adapted, derived, pedagogical, or 补充. Return parallel attributions for purpose and every indexed prerequisite, objective, definition, misconception, pitfall, and quick-reference string. verbatim, adapted, and 补充 require current supplied anchors; derived and pedagogical may omit anchors. Never cite an anchor that was not supplied.
The approved teaching contract requires purpose, prerequisites, objectives, grounded definitions and formulas, stepwise worked examples with independent oracle data, pitfalls, only approved labs, one to three selected exercises with exactly one core and at most one challenge, exactly four hierarchical hints for the core exercise, a transfer task, quick reference, and citations. Every unit-bearing formula or example must include both its produced unit and an independent oracle unit. Physics chapters must include typed vector, direction, reference_frame, boundary, or limit checks with current evidence anchors; mathematics chapters may return an empty physics_checks list.
Never emit executable code, arbitrary HTML, scripts, or unconstrained nested payloads.

{{ instructions }}

Supplied evidence anchors:
{% for item in evidence %}
<anchor>{{ item }}</anchor>
{% endfor %}

{{ format_instructions }}
